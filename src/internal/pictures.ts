export function headpicture(id: string): string {
	return `https://q1.qlogo.cn/g?b=qq&s=140&nk=${id}`;
}
export function grouppicture(id: string): string {
	return `https://p.qlogo.cn/gh/${id}/${id}/`
}
